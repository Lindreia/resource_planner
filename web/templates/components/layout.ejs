<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
    <div class="container-fluid">

        <a class="navbar-brand fw-bold" href="/weekly">
            Resource Planner
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#mainNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto">

                <!-- RESOURCE INPUT (Admin + Manager only) -->
                <% if (user && ["admin", "manager"].includes((user.role || "").toLowerCase())) { %>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'resources' ? 'active fw-bold' : '' %>"
                       href="/assignments/add">
                        Resource Input
                    </a>
                </li>
                <% } %>

                <!-- MANAGER ONLY -->
                <% if (user && (user.role || "").toLowerCase() === "manager") { %>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'manager_dashboard' ? 'active fw-bold' : '' %>"
                       href="/manager/dashboard">
                        Manager Dashboard
                    </a>
                </li>
                <% } %>

                <!-- ADMIN DASHBOARDS -->
                <% if (user && (user.role || "").toLowerCase() === "admin") { %>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'admin_dashboard' ? 'active fw-bold' : '' %>"
                       href="/admin/dashboard">
                        Admin Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'manager_dashboard' ? 'active fw-bold' : '' %>"
                       href="/manager/dashboard">
                        Manager Dashboard
                    </a>
                </li>
                <% } %>

                <!-- WEEKLY -->
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'weekly' ? 'active fw-bold' : '' %>"
                       href="/weekly">
                        Weekly Dashboard
                    </a>
                </li>

                <!-- MONTHLY -->
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'monthly' ? 'active fw-bold' : '' %>"
                       href="/monthly">
                        Monthly Planner
                    </a>
                </li>

                <!-- DAILY -->
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'daily' ? 'active fw-bold' : '' %>"
                       href="/daily">
                        Daily View
                    </a>
                </li>

                <!-- ADMIN ONLY -->
                <% if (user && (user.role || "").toLowerCase() === "admin") { %>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'admin_users' ? 'active fw-bold' : '' %>"
                       href="/admin/users">
                        User Management
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= active_page === 'admin_add_user' ? 'active fw-bold' : '' %>"
                       href="/admin/users/add">
                        Create User
                    </a>
                </li>
                <% } %>

                <!-- USER DROPDOWN -->
                <% if (user) { %>
                <li class="nav-item dropdown ms-3">
                    <a class="nav-link dropdown-toggle fw-bold" href="#" id="userMenu"
                       role="button" data-bs-toggle="dropdown">
                        <%= user.name %> (<%= user.role %>)
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/logout">Logout</a></li>
                    </ul>
                </li>
                <% } %>

            </ul>
        </div>

    </div>
</nav>
