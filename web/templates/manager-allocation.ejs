<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resource Allocation | Resource Planner</title>
    <link rel="stylesheet" href="/static/style.css">
</head>

<body data-theme="light">
<% const adminSuffix = isAdminView ? "?admin_view=1" : ""; %>

<div class="layout">

    <!-- Sidebar -->
    <% if (!isAdminView) { %>
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <span class="sidebar-logo">R8</span>
            <button class="sidebar-toggle" onclick="toggleSidebar()">☰</button>
        </div>

        <nav class="sidebar-nav">
            <a href="/manager/dashboard" class="nav-link">📊 <span class="nav-text">Dashboard</span></a>
            <a href="/manager/projects" class="nav-link">📁 <span class="nav-text">Projects</span></a>
            <a href="/manager/availability" class="nav-link">📅 <span class="nav-text">Availability</span></a>
            <a href="/manager/allocation" class="nav-link active">📦 <span class="nav-text">Allocation</span></a>
            <a href="/manager/team" class="nav-link">👥 <span class="nav-text">Team</span></a>
            <a href="/manager/bookings" class="nav-link">🗂 <span class="nav-text">Bookings</span></a>
            <a href="/logout" class="nav-link logout-link">Logout</a>
        </nav>
    </aside>
    <% } %>

    <!-- Main Content -->
    <main class="content">

        <h2>Resource Allocation</h2>
        <p class="subtitle">Workload and project allocation for your team</p>

        <% if (error) { %>
            <div class="error-box"><%= error %></div>
        <% } %>

        <% if (message) { %>
            <div class="success-box"><%= message %></div>
        <% } %>

        <!-- Filters -->
        <form method="GET" action="/manager/allocation" class="filter-form">
            <% if (isAdminView) { %>
                <input type="hidden" name="admin_view" value="1">
            <% } %>

            <div class="input-group">
                <label>Week Starting</label>
                <input type="date" name="week" value="<%= filters.week || '' %>">
            </div>

            <button class="login-btn small-btn" type="submit">Apply</button>
        </form>

        <!-- Allocation Table -->
        <% if (allocations.length === 0) { %>
            <div class="info-text">No allocation data found for this week.</div>
        <% } else { %>

            <table class="user-table">
                <tr>
                    <th>Team Member</th>
                    <th>Total Hours</th>
                    <th>Capacity</th>
                    <th>Utilization</th>
                    <th>Projects</th>
                    <th>Actions</th>
                </tr>

                <% allocations.forEach(a => { %>
                    <tr>
                        <td><%= a.user_name %></td>
                        <td><%= a.total_hours %> hrs</td>
                        <td><%= a.capacity %> hrs</td>

                        <td>
                            <% 
                                const pct = Math.round((a.total_hours / a.capacity) * 100);
                                const color = pct > 100 ? 'red' : pct > 80 ? 'orange' : 'green';
                            %>
                            <span class="util-<%= color %>"><%= pct %>%</span>
                        </td>

                        <td>
                            <% if (a.projects.length === 0) { %>
                                None
                            <% } else { %>
                                <ul class="project-list">
                                    <% a.projects.forEach(p => { %>
                                        <li><%= p.project_code %> - <%= p.project_name %> (<%= p.hours %>h/wk)</li>
                                    <% }) %>
                                </ul>
                            <% } %>
                        </td>

                        <td class="actions">
                            <% if (a.projects.length === 0) { %>
                                <span class="info-text">No entries</span>
                            <% } else { %>
                                <% a.projects.forEach(p => { %>
                                    <div style="display:flex; gap:8px; margin-bottom:8px; align-items:center; flex-wrap:wrap;">
                                        <a class="login-btn small-btn" href="/manager/allocation/<%= p.assignment_id %>/edit<%= adminSuffix %>">Edit</a>
                                        <form method="POST" action="/manager/allocation/<%= p.assignment_id %>/delete<%= adminSuffix %>" class="inline-form" onsubmit="return confirm('Delete this allocation entry?');">
                                            <button class="login-btn small-btn warn-btn" type="submit">Delete</button>
                                        </form>
                                    </div>
                                <% }) %>
                            <% } %>
                        </td>
                    </tr>
                <% }) %>
            </table>

        <% } %>

        <div class="theme-toggle" onclick="toggleTheme()">🌙 Dark Mode</div>

        <div class="footer-links">
            <a href="/manager/dashboard<%= adminSuffix %>">Back to Manager Dashboard</a>
        </div>

    </main>

</div>

<script>
/* Sidebar collapse */
function toggleSidebar() {
    const s = document.getElementById("sidebar");
    s.classList.toggle("collapsed");
    localStorage.setItem("sidebar-collapsed", s.classList.contains("collapsed"));
}

document.addEventListener("DOMContentLoaded", () => {
    if (localStorage.getItem("sidebar-collapsed") === "true") {
        document.getElementById("sidebar").classList.add("collapsed");
    }
});

/* Theme persistence */
function toggleTheme() {
    const root = document.documentElement;
    const next = root.getAttribute("data-theme") === "dark" ? "light" : "dark";
    root.setAttribute("data-theme", next);
    localStorage.setItem("theme", next);
}

const saved = localStorage.getItem("theme");
if (saved) {
    document.documentElement.setAttribute("data-theme", saved);
}
</script>

</body>
</html>
